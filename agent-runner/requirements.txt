# agent-runner + conductor deps (venv is gitignored; recreate with:
#   uv venv .venv && uv pip install --python .venv/bin/python -r requirements.txt)
claude-agent-sdk>=0.2.114
anyio>=4
psycopg[binary]>=3.3
pyyaml>=6
