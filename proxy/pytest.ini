[pytest]
# Dev-only test config for the proxy. pytest is NOT a runtime dependency and is
# not copied into the container (Dockerfile ships only main.py + routing.py).
asyncio_mode = auto
testpaths = tests
